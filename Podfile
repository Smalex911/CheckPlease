# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'CheckPlease' do
  use_frameworks!
  
  pod 'ViRouting', :git => 'git@gitlab.vigroup.ru:ios/libs/ViRouting.git'
  
end
